{{- /* Personal SSH hosts. None active right now — no 1Password lookups happen,
   so this file applies without any extra items.

   To add the Forge server later: create a 1Password item named
   "<opPrefix> - <machine> - Forge" with a hostname field, then add a Host block
   whose HostName uses the op-read template (see this file's git history for the
   exact line). Keys are served by the 1Password SSH agent (00-defaults). */ -}}

# --- Personal server (Forge) — disabled ---
# Host ajp.net pshosting forge
#     HostName <from 1Password>
#     User forge
